# Отчёт: 002.3 — Service Frontend — Layout передаёт microservicesAccess

**Дата:** 2026-04-23  
**Домен:** 3_service  
**Статус:** ✅ Выполнено (реализовано в 002.1)

---

## Что сделано

Реализовано в рамках тикета 002.1.

Service Layout (`apps/service/frontend/src/components/layout/Layout.tsx`):
- Использует `useAuth()` для получения `user?.microservices`
- Вычисляет `isServiceLocked = microservicesAccess['service'] === false`
- При блокировке Service:
  - Показывает только `lockedMenuItem` с путём `/lock`
  - `hubSettingsItem = undefined`
  - `excludeServices = ['service', 'pulse']`

---

## Изменённые файлы

| Файл | Действие |
|------|----------|
| `apps/service/frontend/src/components/layout/Layout.tsx` | Изменён (в рамках 002.1) |

---

## Проверки

- [x] Service Layout передаёт `microservicesAccess` из user
- [x] Заблокированные сервисы отображаются с замком в Sidebar
