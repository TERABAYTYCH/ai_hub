# Отчёт: Тикет 016.1 - Infrastructure — LockPage

**Дата:** 2026-04-23  
**Исполнитель:** AI  
**Статус:** ✅ Завершён

---

## Цель

Создать компонент LockPage в ui-kit для отображения "Модуль недоступен" при попытке зайти на заблокированный сервис.

---

## Изменения

### Создан: `libs/ui-kit/src/components/LockPage.tsx`

```typescript
interface LockPageProps {
  serviceName?: string;
  returnUrl?: string;
}

export function LockPage({ serviceName = 'модуль', returnUrl = '/' }: LockPageProps) {
  return (
    <div className="d-flex flex-column align-items-center justify-content-center vh-100">
      <i className="bi bi-lock" style={{ fontSize: '5rem', color: '#6c757d' }}></i>
      <h2 className="mt-4">Модуль недоступен</h2>
      <p className="text-muted">
        Для активации {serviceName} обратитесь к администратору
      </p>
      <a href={returnUrl} className="btn btn-primary mt-3">
        На главную
      </a>
    </div>
  );
}
```

### Изменён: `libs/ui-kit/src/index.ts`

Добавлен экспорт:
```typescript
export { LockPage } from './components/LockPage';
```

---

## Проверки

| Проверка | Результат |
|----------|-----------|
| `yarn typecheck` | ✅ OK |
| `yarn test` | ✅ 24 passed |

---

## Следующие шаги

- 016.2: Sidebar — добавить locked items
- 016.3: Layout — добавить microservicesAccess проп

---

## Затраченное время

~5 минут
