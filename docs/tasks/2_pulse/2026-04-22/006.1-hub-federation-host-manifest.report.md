# Отчёт: Тикет 006.1 - Hub → Federation host + manifest

**Дата:** 2026-04-22  
**Исполнитель:** AI + пользователь (доработки)  
**Статус:** ✅ Завершён

---

## Цель

Превратить Hub из Federation consumer в Federation host, чтобы Pulse и Service могли загружать Hub Settings удалённо.

---

## Выполненные изменения

### Файл: `apps/hub/frontend/vite.config.ts`

**Добавлено пользователем:**

```typescript
import { manifestPlugin, serveDistAssetsPlugin } from '@app/plugins';

const exposes: Record<string, string> = {
  './Settings': './src/pages/SettingsPage',
};

plugins: [
  manifestPlugin({
    serviceId: 'hub',
    serviceName: 'Ject Hub',
    moduleMapping: {
      './Settings': { label: 'Settings', icon: 'bi bi-gear', path: '/hub/settings' },
    },
  }),
  serveDistAssetsPlugin(),
  // ...
  federation({
    name: 'hub',
    filename: 'remoteEntry.js',
    exposes,
    // ...
  }),
]
```

---

## Проверки

| Проверка | Результат |
|----------|-----------|
| `http://hub.lvh.me/assets/manifest.json` | ✅ `{"serviceId":"hub","name":"Ject Hub",...}` |
| `http://hub.lvh.me/assets/remoteEntry.js` | ✅ 200 OK, `application/javascript` |
| `yarn typecheck` | ✅ OK |

---

## Доработки пользователя

1. Добавил импорт плагинов из `@app/plugins`
2. Настроил `manifestPlugin` с корректным serviceId и moduleMapping
3. Добавил `serveDistAssetsPlugin`
4. Добавил `exposes` и `filename` в federation конфиг

---

## Следующие шаги

- 006.2: ui-kit → REGISTERED_SERVICES добавить hub
- 006.3: ui-kit → Layout excludeServices
- 006.4: ui-kit → Layout hubSettingsItem

---

## Затраченное время

~15 минут (AI) + доработки пользователя
