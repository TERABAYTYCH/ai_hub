# Отчёт: Тикет 006.4 - ui-kit → Layout hubSettingsItem

**Дата:** 2026-04-22  
**Исполнитель:** AI  
**Статус:** ✅ Завершён

---

## Цель

Добавить проп `hubSettingsItem` в Layout для отображения Hub Settings в конце меню.

---

## Изменения

### Файл: `libs/ui-kit/src/components/layout/Layout.tsx`

Добавлен проп `hubSettingsItem`:
```typescript
export function Layout({
  children,
  serviceName = 'Ject Hub',
  staticMenuItems = [],
  excludeServices = [],
  hubSettingsItem,  // NEW
}: {
  // ...
  hubSettingsItem?: MenuItem;  // NEW
}) {
  // ...
  const menuItems: MenuItem[] = [
    ...staticMenuItems,
    ...serviceMenuItems,
    ...(hubSettingsItem ? [hubSettingsItem] : []),  // NEW
  ];
}
```

### Файл: `libs/ui-kit/src/components/layout/Layout.spec.tsx`

Добавлены тесты:
- hubSettingsItem добавляется в конец меню
- hubSettingsItem не добавляется если не передан
- hubSettingsItem после staticMenuItems и serviceMenuItems

---

## Проверки

| Проверка | Результат |
|----------|-----------|
| `yarn typecheck` | ✅ OK |
| `yarn test` | ✅ 23 passed (+3 новых) |

---

## Следующие шаги

- 006.5: Hub → Layout с excludeServices

---

## Затраченное время

~5 минут
