# Отчёт: Тикет 006.5 - Hub → Layout с excludeServices

**Дата:** 2026-04-22  
**Исполнитель:** AI  
**Статус:** ✅ Завершён

---

## Цель

Обновить Hub Layout для использования excludeServices, чтобы Hub не отображался в своём собственном меню.

---

## Изменения

### Файл: `apps/hub/frontend/src/components/layout/Layout.tsx`

Добавлен `excludeServices={['hub']}`:

```typescript
export function Layout({ children }: { children: ReactNode }) {
  return (
    <UiKitLayout
      serviceName="Ject Hub"
      staticMenuItems={hubMenuItems}
      excludeServices={['hub']}
    >
      {children}
    </UiKitLayout>
  );
}
```

---

## Проверки

| Проверка | Результат |
|----------|-----------|
| `yarn typecheck` | ✅ OK |
| `yarn test` | ✅ 23 passed |

---

## Следующие шаги

- 006.6: Pulse → Layout с Hub Settings
- 006.7: Pulse → App.tsx routes

---

## Затраченное время

~2 минуты
