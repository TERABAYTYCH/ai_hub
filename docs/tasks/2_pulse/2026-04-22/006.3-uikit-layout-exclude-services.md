# Тикет 006.3: ui-kit → Layout excludeServices

**Дата:** 2026-04-22  
**Домен:** 2_pulse  
**Родительский тикет:** 006 (Pulse — добавить Service и Hub в навигацию)

---

## Цель

Добавить проп `excludeServices` в Layout для фильтрации сервисов из динамического меню.

---

## Текущее состояние

Layout отображает все сервисы из REGISTERED_SERVICES.

---

## Требуемое состояние

```typescript
export function Layout({
  children,
  serviceName = 'Ject Hub',
  staticMenuItems = [],
  excludeServices = [],  // NEW
}: {
  children: ReactNode;
  serviceName?: string;
  staticMenuItems?: MenuItem[];
  excludeServices?: string[];  // NEW
}) {
  // ... filter manifests by excludeServices
  const filteredManifests = manifests.filter(m => !excludeServices.includes(m.serviceId));
}
```

---

## Изменения

| Файл | Действие |
|------|----------|
| `libs/ui-kit/src/components/layout/Layout.tsx` | Добавить проп excludeServices и фильтрацию |

---

## Проверки

- [ ] Layout принимает excludeServices
- [ ] Исключённый сервис не попадает в serviceMenuItems
- [ ] `yarn typecheck` — OK
- [ ] `yarn lint` — OK

---

## Зависимости

- 006.2: REGISTERED_SERVICES добавить hub (завершён)
