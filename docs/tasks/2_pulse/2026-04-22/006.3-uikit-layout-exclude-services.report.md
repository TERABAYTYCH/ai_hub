# Отчёт: Тикет 006.3 - ui-kit → Layout excludeServices

**Дата:** 2026-04-22  
**Исполнитель:** AI  
**Статус:** ✅ Завершён

---

## Цель

Добавить проп `excludeServices` в Layout для фильтрации сервисов из динамического меню.

---

## Изменения

### Файл: `libs/ui-kit/src/components/layout/Layout.tsx`

Добавлен проп `excludeServices`:
```typescript
export function Layout({
  children,
  serviceName = 'Ject Hub',
  staticMenuItems = [],
  excludeServices = [],  // NEW
}: {
  children: ReactNode;
  serviceName?: string;
  staticMenuItems?: MenuItem[];
  excludeServices?: string[];  // NEW
}) {
  // Фильтрация манифестов
  const filteredManifests = manifests.filter(
    (m) => !excludeServices.includes(m.serviceId)
  );
  // ...
}
```

### Файл: `libs/ui-kit/src/components/layout/Layout.spec.tsx` (СОЗДАН)

Тесты для excludeServices:
- Фильтрация одного сервиса (pulse исключён, hub и service остались)
- Фильтрация нескольких сервисов
- Показ всех сервисов при пустом excludeServices

### Файл: `libs/ui-kit/jest.config.js`

Добавлен moduleNameMapper для @app/ui-kit:
```javascript
moduleNameMapper: {
  '^@/(.*)$': '<rootDir>/src/$1',
  '^@app/ui-kit$': '<rootDir>/src/index.ts',
  '^@app/ui-kit/(.*)$': '<rootDir>/src/$1',
  // ...
},
```

### Файл: `libs/ui-kit/src/hooks/useMicroserviceManifests.spec.ts`

Обновлён тест для 3 зарегистрированных сервисов (hub, pulse, service).

---

## Проверки

| Проверка | Результат |
|----------|-----------|
| `yarn typecheck` | ✅ OK |
| `yarn test` | ✅ 20 passed (3 new + 17 existing) |

---

## Следующие шаги

- 006.4: ui-kit → Layout hubSettingsItem

---

## Затраченное время

~15 минут
