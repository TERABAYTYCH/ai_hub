# Отчёт: 017.6 — Hub Frontend — MicroservicesSettings

**Дата:** 2026-04-23  
**Домен:** 1_hub  
**Статус:** ✅ Выполнено

---

## Что сделано

### 1. Создан MicroservicesSettings.tsx

**Файл:** `apps/hub/frontend/src/pages/MicroservicesSettings.tsx`

- Страница с переключателями Pulse и Service
- GET `/api/microservices/access` при загрузке
- POST `/api/microservices/access` при сохранении
- Индикатор "Сохранено!" на 2 секунды

### 2. Добавлен route в App.tsx

**Файл:** `apps/hub/frontend/src/App.tsx`

- Добавлен import `MicroservicesSettings`
- Route `/microservices-settings` → `<MicroservicesSettings />` внутри `<Layout>`

---

## Изменённые файлы

| Файл | Действие |
|------|----------|
| `apps/hub/frontend/src/pages/MicroservicesSettings.tsx` | Создан |
| `apps/hub/frontend/src/App.tsx` | Изменён |

---

## Проверки

- [x] Страница `/microservices-settings` работает
- [x] Toggle переключает состояние
- [x] Кнопка "Применить" сохраняет настройки
- [x] `yarn typecheck` — OK
- [x] `yarn lint` — OK
- [x] `yarn workspace @ject-hub/ui-kit test` — 33 passed

---

## Зависимости

- 017.2: Hub Backend — MicroservicesController API ✅

---

## Доработки

**Доработка #1 - 2026-04-23 — Изменения user**

1. В `useDynamicRoutesConfig` для заблокированного сервиса теперь добавляются ОБА route:
   - `{serviceRoot}/lock` — конкретный route для lock page
   - `{serviceRoot}/*` — wildcard для остальных дочерних роутов (редирект)

2. Lock page теперь обёрнут в `ProtectedRoute > Layout > LockPage`

3. Удалены статические `/pulse/lock` и `/service/lock` routes — теперь всё обрабатывается динамически

4. Route `/microservices-settings` добавлен

**Файлы:** `apps/hub/frontend/src/App.tsx`

**Доработка #2 - 2026-04-23 — Layout.tsx user changes**

1. Проверка `isLocked` теперь использует `user?.microservices || microservicesAccess` — приоритет у user.microservices из auth

2. Locked service path изменён с `/{serviceId}` на `/{serviceId}/lock`

**Файлы:** `libs/ui-kit/src/components/layout/Layout.tsx`

**Доработка #2 - 2026-04-23 — Исправлены проблемы user**

1. **Hub Layout** — добавлен пункт меню "Microservices" с иконкой `bi bi-sliders` и путём `/microservices-settings`

2. **MicroservicesSettings** — заменён `axios` на `fetch` с `credentials: 'include'` как в LoginPage:
   - GET `${API_BASE_URL}/microservices/access`
   - POST `${API_BASE_URL}/microservices/access`
   - Добавлена обработка ошибок

**Файлы:**
- `apps/hub/frontend/src/components/layout/Layout.tsx`
- `apps/hub/frontend/src/pages/MicroservicesSettings.tsx`

**Доработка #3 - 2026-04-23 — Исправлен тест Layout.spec.tsx**

Тест ожидал path `/pulse`, но после изменения Layout path теперь `/pulse/lock`. Тест обновлён.

**Файлы:** `libs/ui-kit/src/components/layout/Layout.spec.tsx`

**Доработка #4 - 2026-04-23 — Fresh access data from backend**

Создан хук `useMicroservicesAccess()` который:
1. Возвращает данные из JWT (для начального рендера)
2. Асинхронно запрашивает актуальные данные с `/api/microservices/access`
3. При получении ответа — использует свежие данные

Теперь после изменения настроек администратором, блокировки обновляются без перелогина.

**Файлы:**
- `apps/hub/frontend/src/hooks/useMicroservicesAccess.ts` (новый)
- `apps/hub/frontend/src/App.tsx` (использует хук вместо `user?.microservices`)
- `apps/hub/frontend/src/components/layout/Layout.tsx` (использует хук вместо `user?.microservices`)
