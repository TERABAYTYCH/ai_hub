# Отчёт: 017.5 — Hub Frontend — /lock route

**Дата:** 2026-04-23  
**Домен:** 1_hub  
**Статус:** ✅ Выполнено

---

## Что сделано

### Добавлены /lock routes в App.tsx

**Файл:** `apps/hub/frontend/src/App.tsx`

1. Добавлен импорт `LockPage` из `@ject-hub/ui-kit`
2. Добавлены static routes:
   - `/pulse/lock` → `<LockPage serviceName="Pulse" returnUrl="/pulse" />`
   - `/service/lock` → `<LockPage serviceName="Service" returnUrl="/service" />`

---

## Изменённые файлы

| Файл | Действие |
|------|----------|
| `apps/hub/frontend/src/App.tsx` | Изменён — добавлены /pulse/lock и /service/lock routes |

---

## Проверки

- [x] `/pulse/lock` отображает LockPage с serviceName="Pulse"
- [x] `/service/lock` отображает LockPage с serviceName="Service"
- [x] `yarn typecheck` — OK
- [x] `yarn lint` — OK
- [x] `yarn workspace @ject-hub/ui-kit test` — 33 passed

---

## Зависимости

- 017.4: Hub Frontend — Проверка доступа в App.tsx ✅

---

## Следующий тикет

- 017.6: Hub Frontend — MicroservicesSettings

---

## Доработки

**Доработка #1 - 2026-04-23 — LockPage внутри Layout**

LockPage теперь рендерится внутри Layout (с шапкой и сайдбаром), а не на всю страницу.

**Файлы:** `apps/hub/frontend/src/App.tsx`

**Доработка #2 - 2026-04-23 — Удалена кнопка "На главную" из LockPage**

Убран returnUrl проп и кнопка "На главную" из LockPage, т.к. Layout уже имеет навигацию.

**Файлы:** `libs/ui-kit/src/components/LockPage.tsx`
