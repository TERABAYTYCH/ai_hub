# Отчет 005.2: Глобальный .gitignore и фикс роутинга авторизации

## Выполнено

### Часть 1: Инфраструктура (.gitignore)

Обновлён `.gitignore`:

```gitignore
# Turborepo cache
.turbo/
**/.turbo/

# Turbo logs
turbo-*.log
```

Удалены из индекса turbo-логи:

- `apps/hub/backend/.turbo/turbo-lint.log`
- `apps/hub/backend/.turbo/turbo-typecheck.log`
- `apps/hub/frontend/.turbo/turbo-lint.log`
- `libs/ui-kit/.turbo/turbo-typecheck.log`

### Часть 2: Фронтенд (Auth Guard & Interceptor)

Создан `apps/hub/frontend/src/utils/auth.ts` с функциями:

- `logout()` — очищает токены и редиректит на `/login`
- `handleUnauthorized()` — вызывает logout при 401
- `authFetch()` — обёртка над fetch, автоматически добавляет Authorization header и обрабатывает 401

Обновлён `apps/hub/frontend/src/api/devices.api.ts`:

- Использует `authFetch` вместо прямых `fetch` вызовов
- При получении 401 автоматически происходит logout и редирект на `/login`

Маршрут `/devices` уже защищён в `App.tsx`:

```tsx
<Route path="/devices" element={isAuthenticated ? <DevicesPage /> : <Navigate to="/login" />} />
```

## Проверки

| Проверка   | Результат                        |
| ---------- | -------------------------------- |
| typecheck  | ✅ passed                        |
| lint       | ✅ passed (4 warnings, 0 errors) |
| test/build | ✅ passed                        |

## Файлы

**Созданные:**

- `apps/hub/frontend/src/utils/auth.ts`

**Измененные:**

- `.gitignore`
- `apps/hub/frontend/src/api/devices.api.ts`

**Удалены из индекса:**

- `apps/hub/backend/.turbo/turbo-lint.log`
- `apps/hub/backend/.turbo/turbo-typecheck.log`
- `apps/hub/frontend/.turbo/turbo-lint.log`
- `libs/ui-kit/.turbo/turbo-typecheck.log`
