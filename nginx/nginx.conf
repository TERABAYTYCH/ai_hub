# ЗАПРЕЩЕНО ИЗМЕНЯТЬ ЭТОТ ФАЙЛ!

user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    # Rate limiting zone
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;

    # ============================================
    # HUB FRONTEND (hub.lvh.me)
    # ============================================
    server {
        listen 80;
        server_name hub.lvh.me;

        location / {
            proxy_pass http://hub-frontend:5173;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }

    # ============================================
    # PULSE FRONTEND (pulse.lvh.me)
    # ============================================
    server {
        listen 80;
        server_name pulse.lvh.me;

        root /usr/share/nginx/html/pulse;

        location / {
            proxy_pass http://pulse-frontend:5174;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }

    # ============================================
    # SERVICE FRONTEND (service.lvh.me)
    # ============================================
    server {
        listen 80;
        server_name service.lvh.me;

        location / {
            proxy_pass http://service-frontend:5175;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }

    # ============================================
    # CONTROL FRONTEND (control.lvh.me)
    # ============================================
    server {
        listen 80;
        server_name control.lvh.me;

        location / {
            proxy_pass http://control-frontend:5176;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }

    # ============================================
    # HUB API (api.hub.lvh.me)
    # ============================================
    server {
        listen 80;
        server_name api.hub.lvh.me;

        location / {
            limit_req zone=api_limit burst=20 nodelay;
            proxy_pass http://hub-backend:3000;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Access-Control-Allow-Origin $http_origin;
            proxy_set_header Access-Control-Allow-Credentials true;
            proxy_set_header Access-Control-Allow-Headers $http_access_control_request_headers;
            proxy_set_header Access-Control-Allow-Methods $http_access_control_request_method;
        }

        location /health {
            access_log off;
            return 200 "OK\n";
            add_header Content-Type text/plain;
        }
    }

    # ============================================
    # PULSE API (api.pulse.lvh.me)
    # ============================================
    server {
        listen 80;
        server_name api.pulse.lvh.me;

        location / {
            proxy_pass http://pulse-backend:3001;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Authorization $http_authorization;
        }
    }

    # ============================================
    # SERVICE API (api.service.lvh.me)
    # ============================================
    server {
        listen 80;
        server_name api.service.lvh.me;

        location / {
            proxy_pass http://service-backend:3002;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Authorization $http_authorization;
        }
    }

    # ============================================
    # CONTROL API (api.control.lvh.me)
    # ============================================
    server {
        listen 80;
        server_name api.control.lvh.me;

        location / {
            proxy_pass http://control-backend:3003;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Authorization $http_authorization;
        }
    }
}
