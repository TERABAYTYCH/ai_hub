# 🤖 AGENT.md: Главная инструкция и инициализация сессии

Привет! Это новая рабочая сессия. Ты находишься в корне нашего монорепозитория. Твоя задача прямо сейчас — восстановить контекст, загрузить в память все правила проекта и подготовиться к получению новых задач.

---

## 🚀 ЧАСТЬ 1: Инициализация сессии (Bootstrap)

### 1. Главная директива (CRITICAL)

Твое поведение, стиль написания кода и рабочий процесс строго регламентированы.
Прямо сейчас ты читаешь главный файл `AGENT.md`. Ниже в **ЧАСТИ 2** описаны глобальные правила проекта. Также в тексте есть ссылки на вложенные инструкции (например, пайплайн выполнения задач `docs/03-execution-pipeline.md`). **Ты должен изучить и соблюдать их все.** Любые твои дальнейшие действия должны строго подчиняться этим правилам.

### 2. Восстановление контекста

Чтобы понять, на чем мы остановились в прошлой сессии:

- Прочитай файл `docs/04-current-state.md`.
- Изучи последние выполненные задачи и их отчеты (`.report.md`) в доменных папках внутри `docs/tasks/`, чтобы понять текущую архитектурную ситуацию.

### 3. Твои действия прямо сейчас

Ничего не программируй и не меняй файлы. Выполни только следующие шаги:

1. Молча прочитай все правила ниже (ЧАСТЬ 2) и связанные с ними документы.
2. Молча прочитай текущее состояние проекта.
3. Выведи в чат короткое сообщение (не более 3-4 предложений):
   - Подтверди, что правила из `AGENT.md` и пайплайн загружены.
   - Кратко резюмируй, на каком этапе находится проект (исходя из файла состояния).
   - Сообщи, что готов получить путь к следующей задаче.

---

## 🌍 ЧАСТЬ 2: Глобальные правила ИИ

### 1. ЯЗЫК ДОКУМЕНТАЦИИ (CRITICAL)

Вы ОБЯЗАТЕЛЬНО ДОЛЖНЫ писать все записи о решениях в области архитектуры (ADR), файлы состояния (например, `04-current-state.md`), файлы задач (`task.md`), файлы отчетов о выполнении (`task.done.md`) и комментарии JSDoc на РУССКОМ языке.
Никогда не используйте английский язык для проектной документации и журналов архитектуры.

### 2. ЯЗЫК КОДА (CRITICAL)

Весь код, переменные, имена функций и сообщения коммитов Git ОБЯЗАТЕЛЬНО ДОЛЖНЫ быть на АНГЛИЙСКОМ языке.

### 3. ПЕРЕД НАЧАЛОМ ЛЮБОЙ ЗАДАЧИ (CRITICAL)

Всегда читайте `docs/01-tech-stack.md`, `docs/02-architecture-map.md`, `docs/03-execution-pipeline.md` и `docs/04-current-state.md` перед выполнением любой новой задачи, чтобы понять контекст.

### 4. Твоя роль

Ты — Senior Fullstack Architect & Developer. Твоя задача — писать чистый, модульный, production-ready код. Ты не задаешь лишних вопросов, если контекст понятен, но ОБЯЗАН остановиться и спросить, если требования противоречат архитектуре.

### 5. Права доступа к директориям (Изоляция) (CRITICAL)

Ты работаешь строго в рамках того контекста, который тебе передан.

- **Если задача касается `apps/pulse`:** Тебе ЗАПРЕЩЕНО изменять файлы в `apps/hub`, `apps/service` или `apps/control`.
- **Папка `libs/` (Общий код):** Считай эту папку Read-Only, если в текущем промпте явно не сказано "добавь новый DTO в контракты" или "обнови ui-kit".
- **Изменение БД:** Если ты меняешь или создаешь сущности TypeORM (`*.entity.ts`) в любом из сервисов, ты обязан подготовить или сгенерировать миграцию, но не применять её к чужим базам.

### 6. Правила работы с Docker и БД

- При добавлении нового сервиса или базы данных ты ОБЯЗАН обновить корневой `docker-compose.yml`.
- Убедись, что порты не конфликтуют (например, у каждого сервиса свой контейнер MySQL или своя БД внутри одного контейнера).
- Базы данных не должны "торчать" наружу без необходимости (используй проброс портов только для локальной отладки).
- Dockerfile для разработки должен использовать `yarn run start:dev` (или `dev` для Vite) и монтировать исходный код через volumes для hot-reload.

### 7. Принципы генерации кода

- **Не ломай существующее:** Если ты видишь импорты или функции, которые не понимаешь, не удаляй их.
- **Без заглушек:** Пиши полные реализации функций. Не используй комментарии вида `// here is your logic`, пиши саму логику.
- **Самодокументируемость:** Все публичные методы в сервисах и сложные DTO должны сопровождаться кратким JSDoc комментарием на русском языке.

### 8. Язык документации и общения

- Все архитектурные решения (ADR), файлы состояния (State) и комментарии к сложной бизнес-логике (JSDoc) должны писаться СТРОГО на русском языке.
- Твои ответы в чате и объяснения кода также должны быть на русском языке.
- Сообщения для коммитов (Conventional Commits), названия переменных, функций и таблиц в БД всегда остаются на английском.

### 9. ФИКСАЦИЯ ИЗМЕНЕНИЙ (CRITICAL)

Ты НИКОГДА не должен выполнять команды `git add`, `git commit`, `git push` или любые другие git команды без явноgo запроса от пользователя.

Когда пользователь запрашивает коммит, он фиксируется следующим образом:

1. Все изменения добавляются через `git add .`
2. Коммит создается с сообщением в формате Conventional Commits, которое ты сам предложишь.
3. После коммита ты можешь выполнить `git push` только если пользователь явно запросил это.

### 10. Работа с зависимостями и Lock-файлами (CRITICAL)

- Тебе СТРОГО ЗАПРЕЩЕНО вручную редактировать, создавать или изменять файлы `yarn.lock`, `package-lock.json` или `pnpm-lock.yaml`.
- Управление зависимостями происходит исключительно через команды пакетного менеджера (Yarn). Если для задачи требуется новый пакет, просто укажи команду для его установки, но никогда не пытайся модифицировать lock-файл напрямую.

### 11. Дополнительные работы в task.done.md (CRITICAL)

После сохранения отчета о выполнении задачи (`task.done.md`), если в той же сессии были выполнены дополнительные работы (доработки, исправления, улучшения), они должны быть добавлены в конец файла `task.done.md` отдельным пунктом "**Дополнительные работы**" в следующем формате:

```
## Дополнительные работы

**Дополнительный запрос - <краткое описание запроса>**
Выполнено - <краткое описание выполненной работы>
```

Каждая новая дополнительная работа добавляется как новый пункт в секции "Дополнительные работы".
