interface LockPageProps {
    serviceName?: string;
}
/**
 * LockPage - displayed when user tries to access a locked microservice.
 * Shows lock icon and message.
 */
export declare function LockPage({ serviceName }: LockPageProps): import("react/jsx-runtime").JSX.Element;
export {};
